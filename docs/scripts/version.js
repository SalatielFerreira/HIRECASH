/**
 * Fonte única da versão exibida ao usuário (página de Configuração).
 * IMPORTANTE: a cada entrega, atualize este valor junto com "version" em
 * package.json e uma entrada correspondente no CHANGELOG.md.
 */
export const APP_NAME = 'HireCash';
export const APP_VERSION = '0.32.1';
